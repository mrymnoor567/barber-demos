# Barber Demos

One GitHub Pages repository for all barber lead demos.

- `index.html` = generic demo sent to most leads
- `assets/` = shared CSS/JS
- `prospects/` = personalized demos for serious leads

GitHub Pages: deploy `main` from `/ (root)`.
